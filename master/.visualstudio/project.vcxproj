<Project DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

  <ItemGroup Label="ProjectConfigurations">
    <ProjectConfiguration Include="Debug|x64">
      <Configuration>Debug</Configuration>
      <Platform>x64</Platform>
    </ProjectConfiguration>

    <ProjectConfiguration Include="Release|x64">
      <Configuration>Release</Configuration>
      <Platform>x64</Platform>
    </ProjectConfiguration>

    <ProjectConfiguration Include="Debugable|x64">
      <Configuration>Debugable</Configuration>
      <Platform>x64</Platform>
    </ProjectConfiguration>

    <ProjectConfiguration Include="Allow_Bytecode|x64">
      <Configuration>Allow_Bytecode</Configuration>
      <Platform>x64</Platform>
    </ProjectConfiguration>
  </ItemGroup>

  <PropertyGroup Label="Globals">
    <ProjectGuid>{6C626BD4-B99F-4EA0-B5C4-3C99C1985D31}</ProjectGuid>
    <Keyword>MakeFileProj</Keyword>
    <ProjectName>Yanzari's Mo Poly Repository</ProjectName>
  </PropertyGroup>

  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.Default.props" />

  <!-- Debug -->
  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|x64'" Label="Configuration">
    <ConfigurationType>Makefile</ConfigurationType>
    <UseDebugLibraries>true</UseDebugLibraries>
    <PlatformToolset>v143</PlatformToolset>

    <NMakeBuildCommandLine>echo Debug build not required for Lua scripts</NMakeBuildCommandLine>
    <NMakeReBuildCommandLine>echo Rebuild not required</NMakeReBuildCommandLine>
    <NMakeCleanCommandLine>echo Clean not required</NMakeCleanCommandLine>
  </PropertyGroup>

  <!-- Release -->
  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Release|x64'" Label="Configuration">
    <ConfigurationType>Makefile</ConfigurationType>
    <UseDebugLibraries>false</UseDebugLibraries>
    <PlatformToolset>v143</PlatformToolset>

    <NMakeBuildCommandLine>echo Release build not required for Lua scripts</NMakeBuildCommandLine>
    <NMakeReBuildCommandLine>echo Rebuild not required</NMakeReBuildCommandLine>
    <NMakeCleanCommandLine>echo Clean not required</NMakeCleanCommandLine>
  </PropertyGroup>

  <!-- Debugable -->
  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debugable|x64'" Label="Configuration">
    <ConfigurationType>Makefile</ConfigurationType>
    <UseDebugLibraries>false</UseDebugLibraries>
    <PlatformToolset>v143</PlatformToolset>

    <NMakeBuildCommandLine>echo Debugable mode: no compilation needed</NMakeBuildCommandLine>
    <NMakeReBuildCommandLine>echo Rebuild not required</NMakeReBuildCommandLine>
    <NMakeCleanCommandLine>echo Clean not required</NMakeCleanCommandLine>
  </PropertyGroup>

  <!-- Allow Bytecode -->
  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Allow_Bytecode|x64'" Label="Configuration">
    <ConfigurationType>Makefile</ConfigurationType>
    <UseDebugLibraries>false</UseDebugLibraries>
    <PlatformToolset>v143</PlatformToolset>

    <NMakeBuildCommandLine>echo Bytecode debug mode: no compilation needed</NMakeBuildCommandLine>
    <NMakeReBuildCommandLine>echo Rebuild not required</NMakeReBuildCommandLine>
    <NMakeCleanCommandLine>echo Clean not required</NMakeCleanCommandLine>
  </PropertyGroup>

  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.props" />

  <ItemGroup>
    <None Include="..\Yanzari's Mo Poly\**\*" />
    <None Include="..\Yanzari's Mo Poly++\**\*" />
    <None Include="..\SRB2 2.2 Riders (Revival)\**\*" />
  </ItemGroup>

  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.targets" />

</Project>